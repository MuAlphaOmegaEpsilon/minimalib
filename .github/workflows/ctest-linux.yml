name: CTest (Linux)
on:
  push:
    paths:
      - '.github/workflows/ctest-linux.yml'
      - '**/CMakeLists.txt'
      - '**.cmake'
      - '**.hpp'
      - '**.cpp'
jobs:
  ctest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    env:
      TERM: xterm-256color
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build tests
        run: |
          cmake -B build -S .tests -DCMAKE_CXX_COMPILER=clang++
          cmake --build build
      - name: Run tests
        run: |
          cd build
          ctest --timeout 1 --output-on-failure
